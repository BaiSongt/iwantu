name: next-app-ci

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: iwantu_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d iwantu_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: next-app

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/iwantu_test?schema=public
      JWT_SECRET: ci-only-jwt-secret-do-not-use-in-production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: next-app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Prisma validate
        run: npm run prisma:validate

      - name: Prisma generate
        run: npm run prisma:generate

      - name: Apply migrations
        run: npm run prisma:migrate:deploy

      - name: Invariant tests
        run: npm test

      - name: M4 signed acceptance happy path
        run: node --test --test-name-pattern="signed Buyer acceptance forms" tests/v2-m4-signed-offer-acceptance.test.mjs

      - name: M4 signed acceptance fail-closed signature
        run: node --test --test-name-pattern="tampered Buyer signature" tests/v2-m4-signed-offer-acceptance.test.mjs

      - name: M4 unsigned formation bypass guard
        run: node --test --test-name-pattern="database gate rejects unsigned" tests/v2-m4-signed-offer-acceptance.test.mjs

      - name: M5 signed Delivery happy path
        run: node --test --test-name-pattern="signed Supplier Delivery is immutable" tests/v2-m5-signed-delivery.test.mjs

      - name: M5 signed Delivery fail-closed signature
        run: node --test --test-name-pattern="tampered Supplier Delivery signature" tests/v2-m5-signed-delivery.test.mjs

      - name: M5 Delivery state bypass guard
        run: node --test --test-name-pattern="database rejects direct ACCEPTANCE_PENDING" tests/v2-m5-signed-delivery.test.mjs

      - name: M5 deterministic AUTO_ACCEPT policy
        run: node --test tests/v2-m5-auto-accept.test.mjs

      - name: M5 atomic terminal Settlement
        run: node --test tests/v2-m5-atomic-settlement.test.mjs

      - name: M6 Supplier Default atomic full refund
        run: node --test tests/v2-m6-supplier-default-refund.test.mjs

      - name: M7 REJECT to REWORK second Delivery
        run: node --test tests/v2-m7-rework-second-delivery.test.mjs

      - name: M8 Dispute bilateral MUTUAL_SPLIT
        run: node --test tests/v2-m8-dispute-mutual-split.test.mjs

      - name: M9 terminal ReputationEvidence
        run: node --test tests/v2-m9-terminal-reputation-evidence.test.mjs

      - name: M10 ReputationSnapshot foundation
        run: node --test tests/v2-m10-reputation-snapshot-foundation.test.mjs

      - name: M10 Local and Global Trust
        run: node --test tests/v2-m10-local-global-trust.test.mjs

      - name: M10 Capability Reputation
        run: node --test tests/v2-m10-capability-reputation.test.mjs

      - name: M10 Reputation Passport
        run: node --test tests/v2-m10-reputation-passport.test.mjs

      - name: M10 PostgreSQL rebuild hardening
        run: node --test tests/v2-m10-reputation-rebuild-postgres.test.mjs

      - name: M1 changed-code lint
        run: npm run lint:m1

      - name: M2 changed-code lint
        run: npm run lint:m2

      - name: M3 changed-code lint
        run: npm run lint:m3

      - name: M4 changed-code lint
        run: npm run lint:m4

      - name: M5 changed-code lint
        run: npm run lint:m5

      - name: M6 changed-code lint
        run: npm run lint:m6

      - name: M7 changed-code lint
        run: npm run lint:m7

      - name: M8 changed-code lint
        run: npm run lint:m8

      - name: M9 changed-code lint
        run: npm run lint:m9

      - name: M10 changed-code lint
        run: npm run lint:m10

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      # master currently carries legacy ESLint failures from React/Next rule changes.
      # Keep them visible without making unrelated UI cleanup a prerequisite for M1-M5.
      - name: Legacy full-repository lint baseline
        continue-on-error: true
        run: npm run lint
